import React from "react";
import { GiWorld } from "react-icons/gi";

const Title = () => (
  <div className="title">
    <GiWorld className="icon" />
    <span>A Time Zone App with React</span>
  </div>
);

export default Title;
